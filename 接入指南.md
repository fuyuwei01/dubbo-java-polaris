# dubbo-java-polaris 接入指南

本文说明如何将 **Apache Dubbo 2.7.x** 应用接入 [北极星 Polaris](https://github.com/polarismesh/polaris)，使用服务注册发现、动态路由、负载均衡、配置中心、限流与熔断等能力。

> 当前文档对应分支：`release/2.7/2.1.2.0`  
> 组件版本：`dubbo-java-polaris 2.1.2.0-2.7.23` / Apache Dubbo `2.7.23` / Polaris Java SDK `2.1.2.0` / JDK `1.8+`

---

## 1. 功能一览

| 能力 | 插件 Artifact | 说明 |
| --- | --- | --- |
| 服务注册发现 | `dubbo-registry-polaris` | 实例注册/反注册、订阅发现；Consumer 调用结果上报 |
| 动态路由 / 就近路由 | `dubbo-router-polaris` | 按北极星路由规则过滤实例；支持就近 |
| 负载均衡 | `dubbo-loadbalance-polaris` | 加权随机、加权轮询、一致性哈希、最短响应、最少连接 |
| 配置中心 | `dubbo-configcenter-polaris` | Dubbo 动态配置对接北极星配置中心 |
| 限流 | `dubbo-ratelimit-polaris` | Provider 侧限流（需单独引入） |
| 熔断 | `dubbo-circuitbreaker-polaris` | Consumer 侧熔断（需单独引入） |

> **说明**：元数据上报（`dubbo-metadatareport-polaris`）目前仅 **Dubbo 3.2.x** 分支支持，本 2.7 分支不适用。

---

## 2. 前置准备

1. 部署可用的北极星服务端（或使用已有集群）。
2. 确认默认端口：
   - 服务发现（Discover）：**8091**
   - 配置中心（Config）：**8093**
3. 准备命名空间（未指定时默认 `default`）。
4. 应用侧使用 Apache Dubbo **2.7.x**（本仓库示例为 `2.7.23`）。

命名约定：

| 项 | 约定 |
| --- | --- |
| 命名空间 | 默认 `default`，可通过 `namespace` / `polaris_namespace` 指定 |
| 服务名 | Dubbo **接口全限定名**（FQCN），例如 `com.example.api.HelloService` |
| Registry 协议 | `polaris://{host}:8091` |
| ConfigCenter 协议 | `polaris://{host}:8093` |

本分支 **不依赖** classpath 下的 `polaris.yml`；SDK 配置由 Dubbo Registry / ConfigCenter URL 参数及 BootConfigHandler 注入。

---

## 3. 引入依赖

### 3.1 一站式依赖（推荐起步）

```xml
<dependency>
    <groupId>com.tencent.polaris</groupId>
    <artifactId>dubbo-polaris-all</artifactId>
    <version>2.1.2.0-2.7.23</version>
</dependency>
```

`dubbo-polaris-all` 已包含：`registry`、`router`、`configcenter`、`loadbalance`。

**未包含**（需按需单独引入）：`dubbo-ratelimit-polaris`、`dubbo-circuitbreaker-polaris`。

### 3.2 按需引入

```xml
<!-- 注册发现 + 调用结果上报 Filter -->
<dependency>
    <groupId>com.tencent.polaris</groupId>
    <artifactId>dubbo-registry-polaris</artifactId>
    <version>2.1.2.0-2.7.23</version>
</dependency>

<!-- 路由（含就近） -->
<dependency>
    <groupId>com.tencent.polaris</groupId>
    <artifactId>dubbo-router-polaris</artifactId>
    <version>2.1.2.0-2.7.23</version>
</dependency>

<!-- 负载均衡 -->
<dependency>
    <groupId>com.tencent.polaris</groupId>
    <artifactId>dubbo-loadbalance-polaris</artifactId>
    <version>2.1.2.0-2.7.23</version>
</dependency>

<!-- 配置中心 -->
<dependency>
    <groupId>com.tencent.polaris</groupId>
    <artifactId>dubbo-configcenter-polaris</artifactId>
    <version>2.1.2.0-2.7.23</version>
</dependency>

<!-- 限流（Provider） -->
<dependency>
    <groupId>com.tencent.polaris</groupId>
    <artifactId>dubbo-ratelimit-polaris</artifactId>
    <version>2.1.2.0-2.7.23</version>
</dependency>

<!-- 熔断（Consumer） -->
<dependency>
    <groupId>com.tencent.polaris</groupId>
    <artifactId>dubbo-circuitbreaker-polaris</artifactId>
    <version>2.1.2.0-2.7.23</version>
</dependency>
```

---

## 4. 快速接入（最小配置）

将下面地址中的 `127.0.0.1` 替换为实际北极星地址。

### 4.1 Provider

```properties
dubbo.application.name=my-provider
dubbo.registry.address=polaris://127.0.0.1:8091
dubbo.registry.parameters.namespace=default
dubbo.config-center.address=polaris://127.0.0.1:8093
dubbo.protocol.name=dubbo
dubbo.protocol.port=20880
```

等价 XML：

```xml
<dubbo:application name="my-provider"/>
<dubbo:registry address="polaris://127.0.0.1:8091">
    <dubbo:parameter key="namespace" value="default"/>
</dubbo:registry>
<dubbo:config-center address="polaris://127.0.0.1:8093"/>
<dubbo:protocol name="dubbo" port="20880"/>
```

启动后，在北极星控制台对应命名空间下，应能看到以 **接口 FQCN** 命名的服务及其实例。

### 4.2 Consumer

```properties
dubbo.application.name=my-consumer
dubbo.registry.address=polaris://127.0.0.1:8091
dubbo.registry.parameters.namespace=default
dubbo.registry.parameters.detect_when=after_call
dubbo.config-center.address=polaris://127.0.0.1:8093
dubbo.consumer.filter=polaris_report
dubbo.consumer.loadbalance=polaris_weighted_random
dubbo.consumer.timeout=3000
```

`polaris_report` 用于将调用成功/失败上报至北极星，供熔断、统计等治理能力使用，**建议 Consumer 显式开启**。

---

## 5. 能力配置说明

### 5.1 服务注册发现

协议：`polaris://{host}:8091`

常用参数（挂在 `dubbo.registry` / Registry URL 上）：

| 参数 | 别名 | 说明 | 默认 |
| --- | --- | --- | --- |
| `namespace` | `polaris_namespace` | 北极星命名空间 | `default` |
| `token` | `polaris_token` | 服务鉴权 Token | - |
| `timeout` | `polaris_timeout` | SDK API 超时 | - |
| `persist_enable` | `polaris_persist_enable` | 本地缓存持久化 | - |
| `detect_when` | `polaris_detect_when` | 主动探测时机（如 `after_call`） | - |
| `polaris_other_addresses` | - | 额外 Discover 地址，`/` 分隔 | - |
| `config_port` | `polaris_config_port` | 配置中心端口 | `8093` |
| `stat_type` | `polaris_stat_type` | 监控模式：`push` / `pull` | - |
| `stat_push_addr` | `polaris_stat_push_addr` | Push 地址 | - |
| `stat_push_interval` | `polaris_stat_push_interval` | Push 间隔（ms） | `10000` |
| `stat_pull_port` | `polaris_stat_pull_port` | Pull 端口 | `9091` |

心跳 TTL 可通过系统属性 `ttl` / `polaris_ttl` 配置，默认 `5`。

相关 SPI：

- `RegistryFactory` → `polaris`
- `Filter` → `polaris_report`（Consumer 调用结果上报）

### 5.2 动态路由 / 就近路由

引入 `dubbo-router-polaris` 后，`PolarisRouterFactory`（SPI 名 `polaris_router`）会自动激活。请确保已配置 Polaris Registry，以便初始化治理用 `PolarisOperator`。

就近路由示例：

```properties
dubbo.registry.parameters.polaris_nearby_enabled=true
dubbo.registry.parameters.polaris_nearby_match_level=ZONE
```

`polaris_nearby_match_level` 可选：`REGION` / `ZONE` / `CAMPUS`（大小写不敏感）。

实例地域信息可通过元数据注入（见 [5.7](#57-实例元数据与地域)）。路由规则标签可来自 `HEADER`、`QUERY`（JsonPath）、`PATH`（方法名），规则在北极星控制台配置。

### 5.3 负载均衡

在 Consumer / Reference 上设置 `loadbalance`：

```properties
dubbo.consumer.loadbalance=polaris_weighted_random
```

| SPI 名 | 策略 |
| --- | --- |
| `polaris_weighted_random` | 加权随机 |
| `polaris_weighted_round_robin` | 加权轮询 |
| `polaris_ring_hash` | 一致性哈希 |
| `polaris_shortest_response` | 最短响应时间 |
| `polaris_least_connection` | 最少连接 |

一致性哈希参数索引：

```xml
<dubbo:reference ... loadbalance="polaris_ring_hash">
    <dubbo:parameter key="hash.arguments" value="0"/>
</dubbo:reference>
```

未初始化 Polaris Operator 时，负载均衡会回退为随机策略。

### 5.4 配置中心

```properties
dubbo.config-center.address=polaris://127.0.0.1:8093
# 可选：配置解密（默认开启 AES crypto）
dubbo.config-center.parameters.polaris_config_encrypt_enabled=true
```

映射关系：Polaris `namespace` + Dubbo `group` + `key`（文件名）。

> 注意：若 URL 参数中出现 Dubbo 默认的 `namespace=dubbo`，工厂会移除该参数，避免覆盖北极星命名空间。真正的 Polaris 命名空间请使用 `polaris_namespace`，或使用非 `dubbo` 的 `namespace` 值。Discover 端口可通过 `discover_port` / `polaris_discover_port` 指定（默认 `8091`）。

### 5.5 限流（Provider）

1. 单独引入 `dubbo-ratelimit-polaris`。
2. Filter SPI 名：`polaris_ratelimit`，默认在 **Provider** 侧 `@Activate`。
3. 在北极星控制台配置限流规则（可匹配 HEADER / QUERY）。
4. 超限时抛出 `RpcException`（`LIMIT_EXCEEDED`）。

可选显式声明：

```properties
dubbo.provider.filter=polaris_ratelimit
```

### 5.6 熔断（Consumer）

1. 单独引入 `dubbo-circuitbreaker-polaris`。
2. Filter SPI 名：`polaris_circuitbreaker`，默认在 **Consumer** 侧 `@Activate`。
3. 建议同时开启 `polaris_report`，保证调用结果上报。
4. 在控制台配置熔断规则；限流类异常不会记为熔断失败。

可选显式声明：

```properties
dubbo.consumer.filter=polaris_report,polaris_circuitbreaker
```

### 5.7 实例元数据与地域

注册时写入实例元数据，可用于路由、就近等场景。

**URL 参数：**

- `polaris_metadata_content_{key}`
- `polaris_metadata_transitive_{key}`
- `polaris_metadata_disposable_{key}`

**环境变量：**

- `DUBBO_METADATA_CONTENT_{KEY}`
- `DUBBO_METADATA_CONTENT_TRANSITIVE`
- `DUBBO_METADATA_ZONE` / `DUBBO_METADATA_REGION` / `DUBBO_METADATA_CAMPUS`

位置字段：`region` / `zone` / `campus`。

### 5.8 无损上下线

通过 Registry 参数开启：

| 参数 | 说明 |
| --- | --- |
| `polaris_lossless_enabled` | 开启无损注册 |
| `polaris_lossless_delay_register_interval` | 延时注册间隔 |
| `polaris_lossless_health_check_path` | 健康检查路径（切 DELAY_BY_HEALTH_CHECK） |
| `polaris_lossless_health_check_interval` | 健康检查间隔 |
| `polaris_lossless_admin_port` | Admin 端口 |

```properties
dubbo.registry.parameters.polaris_lossless_enabled=true
dubbo.registry.parameters.polaris_lossless_delay_register_interval=5000
```

### 5.9 与 Nacos 双注册 / 双发现

| 参数 | 说明 | 默认 |
| --- | --- | --- |
| `polaris_nacos_enabled` | 总开关 | `false` |
| `polaris_nacos_server_addr` | Nacos 地址（必填，逗号分隔多地址） | - |
| `polaris_nacos_register_enabled` | 是否注册到 Nacos | `true` |
| `polaris_nacos_discovery_enabled` | 是否从 Nacos 发现 | `true` |
| `polaris_nacos_username` / `polaris_nacos_password` | 鉴权 | - |
| `polaris_nacos_namespace` | Nacos 命名空间 | `public` |
| `polaris_nacos_group` | Group | `DEFAULT_GROUP` |
| `polaris_nacos_cluster` | Cluster | `DEFAULT` |
| `polaris_nacos_ephemeral` | 临时实例 | `true` |
| `polaris_nacos_weight` | 权重 | `1` |
| `polaris_nacos_context_path` | Context Path | - |
| `polaris_nacos_dubbo_adapt` | Dubbo 服务名适配 | `true` |

开启 `polaris_nacos_dubbo_adapt` 后，Nacos 侧服务名格式为：

```text
providers:{interface}:{version}:{group}
```

```properties
dubbo.registry.parameters.polaris_nacos_enabled=true
dubbo.registry.parameters.polaris_nacos_server_addr=127.0.0.1:8848
```

---

## 6. 示例工程

仓库内提供可运行示例（`dubbo-examples`）：

### 6.1 Quickstart（Annotation + properties）

路径：`dubbo-examples/dubbo-quickstart-example`

1. 修改 Provider / Consumer 的 `spring/dubbo-*.properties`，将北极星地址改为实际环境。
2. 启动 Provider：

```bash
mvn -pl dubbo-examples/dubbo-quickstart-example/dubbo-quickstart-provider -am package
# 运行主类
# com.tencent.polaris.dubbo.quickstart.example.provider.Main
```

3. 启动 Consumer：

```bash
# 主类 com.tencent.polaris.dubbo.quickstart.example.consumer.Main
curl 'http://127.0.0.1:15700/echo?method=sayHello&value=world'
```

控制台中可见类似服务名：`com.tencent.polaris.dubbo.example.api.GreetingService`（namespace=`default`）。

### 6.2 Spring Cloud Tencent 示例

路径：`dubbo-examples/dubbo-sct-example`

依赖 `dubbo-registry-polaris` 与 `spring-cloud-starter-tencent-all`，通过 `application.yml` 配置 `dubbo.registry.address` 与 `spring.cloud.polaris`。

```bash
mvn -pl dubbo-examples/dubbo-sct-example/dubbo-sct-provider -am spring-boot:run
mvn -pl dubbo-examples/dubbo-sct-example/dubbo-sct-consumer -am spring-boot:run
# Consumer: GET http://127.0.0.1:18080/hello?name=world
```

### 6.3 API 模块

`dubbo-examples/dubbo-api-example` 仅提供接口定义，无启动入口。

---

## 7. 接入检查清单

1. Maven 依赖版本与 Dubbo 大版本匹配（本分支为 `*-2.7.23`）。
2. Provider / Consumer 的 `dubbo.registry.address` 指向同一 Polaris Discover。
3. 命名空间一致；控制台能看到接口 FQCN 服务与健康实例。
4. Consumer 配置 `polaris_report`（以及按需的 loadbalance / circuitbreaker）。
5. 限流、熔断、路由规则已在控制台下发到正确命名空间与服务。
6. 就近路由场景已设置实例 `region` / `zone` / `campus`，并开启 `polaris_nearby_enabled`。

---

## 8. 与 Dubbo 3.2.x 的差异（简要）

| 维度 | 本分支（2.7） | 3.2.x |
| --- | --- | --- |
| 版本号示例 | `2.1.2.0-2.7.23` | 另一套 `*-3.2.x` revision |
| 元数据上报 | 不支持 | 支持 `dubbo-metadatareport-polaris` |
| 应用级服务发现 | 无 | 有 ServiceDiscovery 扩展 |
| 独立负载均衡插件 | 有 | 插件结构不同 |
| SPI 资源路径 | `META-INF/dubbo/...` | 多为 `META-INF/dubbo/internal/...` |

请按 Dubbo 大版本选择对应分支与文档，勿混用依赖。

---

## 9. 常见问题

**Q: 控制台看不到服务？**  
检查 Registry 地址与端口（8091）、命名空间，以及 Provider 是否成功暴露；服务名是接口 FQCN 而非 `dubbo.application.name`。

**Q: Consumer 调不通？**  
确认双方 namespace 一致、实例健康、网络可达；开启 `detect_when=after_call` 可辅助剔除异常实例。

**Q: 限流 / 熔断不生效？**  
确认已单独引入对应插件，Filter 已激活，且控制台规则作用在正确的服务与标签上；熔断场景建议同时配置 `polaris_report`。

**Q: 配置中心读不到配置？**  
确认地址使用 8093；Polaris namespace 不要被 Dubbo 默认 `namespace=dubbo` 覆盖，优先使用 `polaris_namespace`。

**Q: 是否需要 `polaris.yml`？**  
本 2.7 分支默认不需要；通过 Dubbo URL 参数即可完成接入。

---

## 10. 参考

- 仓库：[polarismesh/dubbo-java-polaris](https://github.com/polarismesh/dubbo-java-polaris)
- 北极星：[polarismesh/polaris](https://github.com/polarismesh/polaris)
- Apache Dubbo：[apache/dubbo](https://github.com/apache/dubbo)
- 示例代码：`dubbo-examples/`
